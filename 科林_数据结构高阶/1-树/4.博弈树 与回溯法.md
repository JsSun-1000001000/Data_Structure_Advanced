
甲乙选数字123 谁先到6谁赢
甲选什么必胜

现在的博弈树只考虑有限步骤

## GameTree

用博弈树要满足一下要求：
1. 全信息：双方信息都是公开且透明的
2. 非偶然：甲乙双方有脑子，做理性选择
3. 零和：必须有人赢有人输/平局

### MAX/MIN极大极小搜索树


### 优化：α-β剪枝


## 回溯法BackTracking

暴力解法
### 回溯法经典解题模板

```cpp
void BackTracking(数据, 结果){
	if(结束条件){
		结果收集
		return;
	}
	for(可能性选择){
		选择
		BackTracking();
		撤销选择
	}
}
```
子集/排列/集合/棋盘/8皇后/n皇后/迷宫 问题

leetcode全排列Permutation 46题

```cpp
class Solution {

public:

    //全排列 nums 结果集数组 哪个位置后开始进行全排列处理

    void backtracking(vector<int>&nums, vector<vector<int> > &ans, int begin){

        //检查 结束了吗 个数与长度相等了吗

        if(begin == nums.size()){

            ans.push_back(nums);

            return;

        }

        //遍历剩余可能性

        for(int i = begin; i < nums.size(); i++){

            //选择

            swap(nums[i], nums[begin]);

            //对剩余元素全排列

            backtracking(nums, ans, begin+1);

            //撤销选择

            swap(nums[i], nums[begin]);

        }

    }

    vector<vector<int>> permute(vector<int>& nums) {  

        //结果集数组 二维数组

        vector<vector<int> > ans;

  

        //哪个位置后开始进行全排列处理

        backtracking(nums, ans,  0);

        return ans;

    }

};
```



