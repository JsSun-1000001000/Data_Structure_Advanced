#### 前言
链表和数组
数组：空间连续 长度固定，基于下标的搜索可以$O(1)$，增删麻烦
链表：增删灵活，查找麻烦

要是可以结合一起就好了。。。

# Hash Table（散列表）

功能：无序数据，快速搜索$O(1)$

哈希表可以直接使用 `unordered_map / unordered_set` 

## hash table的底层实现

散列：按照数据的某种特性进行分组

**建哈希表：**
1. 确定==散列函数==
2. 


